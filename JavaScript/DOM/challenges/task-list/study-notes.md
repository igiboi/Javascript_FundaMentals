Guidelines how to answer precisely 

Did I name the specific thing? (tasks array, not "the data")
Did I say what it does AND why? (not just "it filters" but "it returns a new array excluding...")
Would a stranger understand this without seeing the code?


1.) What does this line do and why?
tasks = tasks.filter((_, index) => index !== Number(buttonIndex));



2.) What is event delegation and why did you use it here instead of attaching a listener directly to each delete button?



3.) What is a closure and where did you use one in this app?



4.) What is the difference between const and let, and why did you have to change tasks from const to let?



5.) What is the difference between event.target and this in an event listener?