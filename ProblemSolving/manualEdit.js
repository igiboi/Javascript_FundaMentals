function manualEdit(arr, action, value) {
    if (action === 'push') {
        return (arr[arr.length] = value);
    }
}

// manualEdit
// if push
    // arr[arr.length] = value;
// else if shift
//grab the first index
// loop foward through array
// add